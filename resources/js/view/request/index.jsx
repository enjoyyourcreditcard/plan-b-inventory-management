import { useState, useEffect } from "react";
import ReactDOM from "react-dom";

const Request = () => {
    return (
        <>
            


            
        </>
    );
}

export default Request;

if (document.getElementById("request-index")) {
    ReactDOM.render(<Request />, document.getElementById("request-index"));
}